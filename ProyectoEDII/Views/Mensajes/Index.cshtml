@model IEnumerable<ProyectoEDII.Models.Mensajes>

@{
    Layout = null;
    ViewBag.Emisor = "Edgar";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Mensaje", "Mensajes");
    </p>

    @*<h2>Write EMISOR:</h2>*@
    @*<input id="emisor" name="emisor" value="GJEIJFEI" />*@
    <p>
        @Html.ActionLink("Reload", "Index", "Mensajes", new {emisor="Genesis"});
    </p>

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Emisor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Receptor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mensaje)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Emisor)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Receptor)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Mensaje)
                </td>
                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                        @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
                </td>
            </tr>
        }

    </table>
</body>
</html>
